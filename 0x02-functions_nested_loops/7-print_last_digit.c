#include "main.h"

/**
 * print_last_digit - this function prints the last digit of a number
 * @num: the number inputted
 * Return: return the last digit on successful
 */

int print_last_digit(int num)
{
	return (num % 10);
}
