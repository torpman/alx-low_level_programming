#include "main.h"
/**
 * add - this function add two integers and return the result
 * @num1: the first integer
 * @num2: the second integer
 * Return: wll return the result on successful execution
 */

int add(int num1, int num2)
{
	return (num1 + num2);
}
