#include <stdio.h>

/**
 * main - entry point of the code
 *
 * Description: the program print single numbers from  0 - 10
 *
 * Return: return 0 on successful execution
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf('\n');
	return (0);

}
