#include <stdio.h>
/**
 * mian - entry point
 * Description: Print all possible combination for a siongle digit numbers
 * return: 0
 */

int main(void)
{
	int = 0;

	while (i < 10)
	{
		putchar(i + '0');
		if(i < 9)
		{
			putchar(44);
			putchar(32);
		}
		i++;
	}
	putchar('\n');
	return (0);
}
